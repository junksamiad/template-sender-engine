# Template Sender Engine

A template engine for sending communications through multiple channels.

## Project Structure

- `/hld` - High-level design documents
- `/lld` - Low-level design documents
- `/implementation` - Implementation code 